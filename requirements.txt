# Fixando numpy < 2 para evitar conflitos com torch/whisper
numpy<2
torch==2.5.1
openai-whisper==20231117
# FastAPI para a API e servir arquivos estáticos
fastapi>=0.68.0,<0.69.0
uvicorn>=0.15.0,<0.16.0
# Para servir arquivos estáticos (WebUI)
aiofiles>=0.5.0,<0.6.0
# Para parsing de formulários multipart (upload)
python-multipart>=0.0.5,<0.1.0
# Para notificações via Telegram
requests>=2.25.0,<3.0.0
# Para manipulação de datas/horas
python-dateutil>=2.8.0,<3.0.0
# Adicionando typing-extensions para compatibilidade
typing_extensions>=3.10.0; python_version < "3.10"